cmake_minimum_required(VERSION 3.12)

project(blobify-download NONE)

include(ExternalProject)
ExternalProject_Add(blobify
  GIT_REPOSITORY    https://github.com/neobrain/blobify.git
  GIT_TAG           master
  SOURCE_DIR        "${CMAKE_BINARY_DIR}/blobify-src"
  BINARY_DIR        "${CMAKE_BINARY_DIR}/blobify-build"
  CONFIGURE_COMMAND ""
  BUILD_COMMAND     ""
  INSTALL_COMMAND   ""
  TEST_COMMAND      ""
)